# Python program to find  the area of triangle

#Firstly take three input 
a = float(input("Enter the nubmer here : "))
b = float(input("Enter the nubmer here : "))
c = float(input("Enter the nubmer here : "))


# Formula for calculate the semi-perimeter
s = (a + b + c) / 2

# Calculate  the area

area = (s * (s-a) * (s-b) * (s-c) ** 0.5)

print("The areas of the triangle is %0.3f" %area)
